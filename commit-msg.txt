feat(import/export): importar/exportar JSON y arreglo del borrado de partidos

- Partidos Guardados: el Exportar de cada fila genera ahora el JSON de copia
  (mismo formato que el EXPORTAR de ACCIONES, sin campo id en el payload);
  el Importar de la cabecera acepta .json y crea un partido nuevo a partir del
  archivo (equipos, jornada y payload completo), sustituyendo al importador de Excel.
- ACCIONES: reorganizados los botones (PÉRDIDAS con aviso IGUALAR arriba,
  EXPORTAR con badge OK COPIA, IMPORTAR junto a NOTA DE VOZ); eliminados el
  EXPORTAR grande (ya solo autoguarda), el IMPORTAR de Excel y el botón de
  memoria COPIA <fecha>.
- FIN 2ª PARTE: el confirm del navegador se sustituye por un modal "GUARDAR
  DATOS" con opciones EXPORTAR y CANCELAR.
- fix(borrar partido): cierra el partido abierto antes de borrar (el
  autoguardado lo recreaba), verifica con get() que la ruta quedó vacía y
  avisa si quedan partidos duplicados con la misma jornada.
- fix(lista de partidos): la lista usa la clave de Firebase como id
  ({...val, id}); antes el id interno importado desde un JSON mandaba sobre la
  clave real y el borrado iba a una ruta inexistente. Se limpia automáticamente
  el campo id interno de los partidos que lo tengan.
- fix(resetMatchData): reinicia también pérdidas, transición, tiros a puerta y
  tiros a puerta del rival al crear un partido.
- GOLES: nueva opción ACCION INDIVIDUAL en el desplegable ORIGEN.
