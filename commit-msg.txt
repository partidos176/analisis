fix(minutos jornada): colorear todos los J+NÚMERO en rosado (regex)

- Antes solo "J2" se ponía en rosado; ahora se usa split con regex `/(J\d+)/`
  para detectar cualquier "J" seguido de dígitos (J1, J2, J3, J10, etc.)
- Cada coincidencia se envuelve en <span style={{color:'#f472b6'}}>...</span>
- El resto del nombre de la jornada (equipos, separadores) queda en blanco