fix(minutos jornada): corregir renderizado J2 en rosado (evitar [object Object])

- El `.replace('J2', <span>...)` en string convertía el elemento React a
  "[object Object]" en pantalla
- Cambiado a `split('J2')` + mapeo de partes con `<span style={{color:'#f472b6'}}>J2</span>`
  intercalado, renderizando correctamente solo "J2" en rosado